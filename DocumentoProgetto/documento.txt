PROGETTO ASD 2025/2026 Luca Montanelli 690125

Analisi di grafi di Autonomous Systems (AS).


Obiettivo del progetto:

L'obiettivo del progetto è la costruzione e l'analisi di un grafo pesato, costruito a partire
da dati BGP relativi agli Autonomous Systems (AS) di Internet.
Lo scopo finale è progettare degli algoritmi che permettano, dati due nodi u e v,
di trovare il costo del cammino minimax ottimo da u a v.
Il costo di un cammino è definito come il massimo peso di un arco lungo quel cammino.


Nel grafo:
- ogni nodo rappresenta un Autonomous System;
- ogni arco rappresenta una connessione osservata nei percorsi BGP;
- il peso di un arco rappresenta la frequenza con cui la connessione compare nei percorsi osservati.



Prima idea di base del progetto completo:

L'idea è dividere il lavoro in moduli separati:

- lettura dei dataset;
- costruzione del grafo;
- gestione delle frequenze sugli archi;
- ricerca della componente connessa maggiore;
- costruzione di un Minimum Spanning Tree;
- risoluzione del problema minimax;
- verificare il corretto funzionamento con piccoli test.


Si costruisce un Minimum Spanning Tree perché si intende sfruttare una proprietà data
dalla sua costruzione, ossia che in un MST il cammino fra due nodi minimizza il valore
del massimo arco attraversato.



Strutture dati che verrano utilizzate:

- liste di adiacenza per rappresentare il grafo;
- hash tables (potenzialmente universal) per memorizzare le frequenze degli archi;
- eventualmente altre strutture per la parte successiva 
  alla gestione delle frequenze sugli archi.




Organizzazione dei file del programma:

Il progetto sarà organizzato in moduli separati, ciascuno suddiviso in:

- un file ".hpp", che fungerà da header per essere letto dal main;
- un file ".cpp", sostanzialmente identico al precedente, ma essendo .cpp 
  permette di eseguire direttamente quel modulo qualora ce ne fosse bisogno.

Questa suddivisione permette ai vari moduli del progetto di comunicare tra loro tramite
il comando "#include "file.hpp"", inoltre il progetto sarà più leggibile, 
e dunque più facile da rimaneggiare nel tempo.

In particolare, il modulo principale "main.cpp" utilizzerà gli header ".hpp" 
per accedere alle funzioni implementate negli altri moduli.

Ho scelto di mettere per adesso all'interno dei file.hpp anche le implementazioni,
e non più solo le dichiarazioni, per facilitare l'esecuzione del programma da terminale.


I moduli saranno:

- "LettoreDataset";
- "CostruzioneGrafo";
- "GestioneFrequenze";
- "ComponentiConnesse";
- "ControlloDSU";
- "CostruzioneMST";
- "RisoluzioneMinimax".







Di seguito sono fornite spiegazioni dettagliate per ogni modulo.

-"main":
  Al momento il main è stato usato per una piccola simulazione al fine di verificare
  la funzionalità dei moduli "LettoreDataset","CostruzioneGrafo","GestioneFrequenze", "ComponentiConnesse",
  "ControlloDSU" e un primo pezzo di "CostruzioneMST".

  Il main:
  -grazie all'include riesce a vedere i vari moduli;
  -prende in input "datasetprova.txt" dalla cartella FileInput;
  -legge il dataset grazie alla funzione implementata in LettoreDataset.hpp;
  -stampa il risultato ottenuto, ossia i cammini presenti in datasetprova.txt;
  -costruisce e stampa gli archi e il grafo grazie alle funzioni implementate in CostruzioneGrafo.hpp;
  -costruisce e stampa gli archi pesati grazie alle funzioni implementate in GestioneFrequenze.hpp;
  -trova la componente connessa maggiore grazie alle funzioni implementate in ComponentiConnesse.hpp;
  -simula delle operazioni sui nodi e verifica che la DSU ne tenga traccia con le funzioni implementate in ControlloDSU.hpp;
  -seleziona e stampa gli archi della CCM grazie alle funzioni implementate in CostruzioneMST.hpp.



-"LettoreDataset":
  Ho implementato una prima versione. Il modulo legge un file di testo, 
  contenente cammini BGP rappresentati come sequenze di interi separati da spazi, e li
  traduce in vector di interi.


  ESEMPIO:

  Riga del dataset: 1 2 5
  Rappresenta il cammino: 1 -> 2 -> 5
  Viene tradotta in: [1,2,5]


  IL MODULO:

  -apre il file;
  -legge una riga alla volta;
  -estrae i numeri presenti nella riga;
  -aggiunge ogni numero al vector del cammino corrente;
  -terminati i numeri della riga, salva il cammino dentro il vector di vector cammini.


  SRUTTURE DATI UTILIZZATE:

  -vector<int> per un singolo cammino;
  -vector<vector<int>> per l'insieme di tutti i cammini.

  
  COMPLESSITÀ:

  La complessità è chiaramente lineare col numero di cammini nel file in input.




-"CostruzioneGrafo":
  Nella sua prima versione, 
  

  IL MODULO:

  CostruzioneGrafo ha delle funzioni che, oltre a stampare, gli permettono di fare principalmente due cose:

  -Prende ogni cammino letto dal dataset e lo traforma in archi("costruisciArchi");
    Un cammino [a,b,c,d] verrà trasformato negli archi (a,b),(b,c),(c,d);
    Successivamente vengono stampati tutti gli archi con "stampaArchi".
  -Trasforma questi archi in un grafo, rappresentato tramite liste di adiacenza("costruisciGrafo");
    Dato che il grafo è non orientato bisogna prestare attenzione alle ripetizioni, e per questo c'è "ricerca";
    Successivamente viene stampato il grafo con "stampaGrafo".


  STRUTTURE DATI UTILIZZATE:

  -vector<pair<int,int>> per memorizzare gli archi del grafo;
  -vector<vector> per rappresentare il grafo tramite liste di adiacenza.


  COMPLESSITÀ:

  Se N è il numero di nodi del grafo, e M è il numero totale degli archi
  (notare che per un cammino lungo d ci sono M-1 archi)
  allora:

  -La funzione "costruisciArchi" scorre tutti i cammini e costruisce gli archi corrispondenti;
    Tale operazione richiede complessità O(M).
  -La funzione "costruisciGrafo" scorre tutti gli archi presenti nel vector archi;
     Prima di ogni inserimento viene eseguita "ricerca";
       Nel caso peggiore, un nodo potrebbe essere connesso a tutti gli altri nodi del grafo,
       quindi la ricerca può richiedere tempo O(N);
     Poiché tale operazione viene effettuata per ciascun arco,
     la complessità totale della costruzione del grafo risulta O(M * N).




-"GestioneFrequenze":
  Ho implementato una prima versione del modulo. Il suo ruolo è determinare quante volte 
  ciascun arco compare all'interno dei cammini del dataset.
  Tali frequenze verranno successivamente utilizzate come pesi degli archi del grafo.


  ESEMPIO:

  con archi:{(1,2),(2,5),(1,2),(2,7),(2,1)}
  Gli archi pesati risultano:{(1,2) peso 3,(2,5) peso 1,(2,7) peso 1}


  IL MODULO:

  -scorre tutti gli archi presenti nel vector archi;
  -controlla se l'arco è già presente tra gli archi pesati e nel caso in che posizione;
  -se presente aumenta di 1 il peso dell'arco pesato che si trova in quella posizione;
  -se non presente crea un nuovo arco pesato con peso 1 e lo aggiunge ad archipesati.


  STRUTTURE DATI UTILIZZATE:

  -struct ArcoPesato, contenente:{int nodo1;int nodo2;int peso} per rappresentare un arco pesato;
  -vector<ArcoPesato> per rappresentare tutti gli archi con il relativo peso.


  COMPLESSITÀ:

  Se M è il numero totale di archi presenti nel vector archi, allora:

  Per ogni arco viene effettuata una ricerca nel vector archipesati. Nel caso peggiore 
  ogni nuovo arco risulta diverso dai precedenti, quindi il numero totale di confronti risulterebbe:
  
  0 + 1 + 2 + 3 + ... + M = M(M+1)/2, Pertanto la complessità del modulo risulta O(M²).



-"ComponentiConnesse":

  Ho implementato una prima versione del modulo. Il suo ruolo è individuare
  le componenti connesse del grafo costruito nei moduli precedenti.


  ESEMPIO:

  Grafo:
  1-2-3
  4-5
  6-7-8

  Le componenti connesse risultano:

  {1,2,3}
  {4,5}
  {6,7,8}


  IL MODULO:

  -scorre tutti i nodi del grafo;
  -quando incontra un nodo non ancora visitato e che abbia almeno 1 vicino, avvia una visita DFS;
  -durante la DFS vengono raggiunti tutti i nodi appartenenti alla stessa componente, con chiamate ricorsive;
  -i nodi trovati vengono salvati in un vector temporaneo;
  -terminata la DFS, il vector viene aggiunto al vector contenente tutte le componenti.


  STRUTTURE DATI UTILIZZATE:

  -vector<bool> visitato, che memorizza se un nodo è già stato visitato;
  -vector<int> per memorizzare una singola componente connessa;
  -vector<vector<int>> per memorizzare tutte le componenti del grafo.


  COMPLESSITÀ:

  Se N è il numero di nodi del grafo e M è il numero degli archi, allora:

  -ogni nodo viene visitato una sola volta;
  -ogni arco viene esplorato una sola volta durante la DFS.

  Quindi la complessità del modulo risulta O(N + M).




-"ControlloDSU":
  
  Ho implementato una prima versione del modulo. Questo modulo svolge esclusivamente un ruolo di appoggio
  e aiuto al modulo successivo, "CostruzioneMST". Infatti, dopo aver trovato la componente connessa maggiore,
  si vorrebbe costruire il Minimum Spanning Tree con gli archi pesati già trovati, ed è qui che sorge il problema
  che questo modulo è adibito a risolvere: 
  sapere se i due estremi dell'arco appartengano già allo stesso insieme di nodi dell'albero costruito 
  fino a questo momento. Questa informazione è importante perché se la risposta è negativa, posso tranquillamente
  inserire l'arco nel MST, e aggiornare la DSU unendo i due insiemi distinti, ma se la risposta è positiva 
  significa che non posso aggiungere l'arco perché altrimenti si formerebbe un ciclo.
  

  NEL MODULO:

  Ci saranno vari insiemi di nodi, per ognuno memorizzo il proprio "padre", 
  cioè il suo nodo di riferimento, e nel caso in cui il padre di un nodo sia il nodo stesso, 
  allora quello si chiamerà "rappresentante" di quell'insieme di nodi.
  Quando si vuole aggiungere, nella costruzione dell'MST, un arco (a,b), 
  il modulo verifica che a e b appartengano a due insiemi disgiunti, 
  se è così unisce i due insiemi,
  sostituendo il campo "padre" del rappresentante di b con il rappresentante dell'insieme a cui appartiene a.  
  

  ESEMPIO:

  Immaginando che la componente connessa maggiore sia {1,2,3,4} 
  Inizialmente ogni nodo è un insieme disgiunto, quindi avrà sè stesso come rappresentante (quindi come padre),
  cioè inizialmente:
    Nodo: 1, Padre: 1
    Nodo: 2, Padre: 2
    Nodo: 3, Padre: 3
    Nodo: 4, Padre: 4
  poi, immaginando di voler aggiungere l'arco (1,2):
    Nodo: 1, Padre: 1
    Nodo: 2, Padre: 1
    Nodo: 3, Padre: 3
    Nodo: 4, Padre: 4
  infatti adesso 2 fa parte dello stesso insieme di 1, e avrà dunque lo stesso rappresentante.
  Immaginando ora di aggiungere l'arco (2,3):
    Nodo: 1, Padre: 1
    Nodo: 2, Padre: 1
    Nodo: 3, Padre: 1
    Nodo: 4, Padre: 4
  Se adesso si volesse aggiungere l'arco (1,3) il modulo osserverebbe che entrambi i nodi hanno
  lo stesso rappresentante, perciò non può unire i due insiemi (in quanto è un unico insieme),
  e ciò di conseguenza, nel modulo successivo, impedirà di aggiungere l'arco al MST, evitando cicli. 
 

  IL MODULO:
   
  ha varie funzioni:
  -(1)void inizializzaDSU: inizializza la DSU prendendo ogni nodo semplice della componente connessa maggiore,
   traformandolo in un NodoDSU, cioè un nodo con padre, dove il padre è il nodo stesso,
   e aggiungendo questo nodo al vector contenente tutti i nodi, nodiDSU.
  -(2)int trovaPosizioneNodo: il nodo i non sarà per forza nella posizione i (o i-1) del vettore nodiDSU,
   potrebbe essere ovunque, e dato che serve sapere la posizione per le successive operazioni, 
   uso una funzione che la trovi. Questa funzione prende in input un nodo, scorre sull'indice i 
   tutto il vector nodiDSU e restituisce il valore di i per cui vale nodiDSU[i].nodo == nodoininput.
  -(3)int trovaRappresentante: questa funzione ricorsiva prende in input un nodo e guarda il suo padre:
   se è il nodo stesso, allora, per definizione, è il rappresentante, e lo restituisce, altrimenti
   richiama la funzione sul padre di quel nodo. Andando avanti così, per le proprietà di costruzione,
   si arriverà sempre a trovare un rappresentante.
  -(4)void unisci: prende in input due nodi e ne trova i rispettivi rappresentanti con la funzione precedente.
   Se i rappresentanti sono uguali non fa niente, altrimenti sostituisce il campo "padre" del rappresentante
   del secondo elemento con il rappresentante del primo elemento. Si cambia il padre di rappresentante2 
   e non di nodo2 perché così tutti gli elementi che avevano come rappresentante rappresentante2, 
   quindi che appartenevano al secondo insieme, adesso avranno rappresentante1 come rappresentante,
   e faranno tutti parte del primo insieme. Altrimenti solo nodo2 ed i suoi figli si sarebbero spostati 
   nel primo insieme,
   lasciando tutti gli altri nodi del secondo insieme lì dentro (anche se questo non deve esistere più).
  -(5)void stampaDSU: stampa i nodi ed i rispettivi padri al momento della chiamata.


  STRUTTURE DATI UTILIZZATE:

  -struct NodoDSU, contenente {int nodo, int padre}, per rappresentare un nodo col suo padre associato;
  -vector<NodoDSU>, per memorizzare tutti i nodi con padre associato.


  COMPLESSITÀ:

  Se N è il numero di nodi della componente connesse maggiore, allora:

  -la funzione (1) visita tutti i nodi una sola volta e compie numero costante di operazioni per iterazione,
   dunque avrà complessità O(N);
  -la funzione (2) effettua una scansione lineare del vettore nodiDSU. Nel caso peggiore il nodo cercato 
   si trova alla fine o non c'è, quindi deve scorrere tutto il vettore, perciò avrà complessità O(N);
  -la funzione (3) risale ricorsivamente la catena dei padri. Ad ogni chiamata ricorsiva viene però chiamata 
   anche la funzione (2), la quale costa O(N). Nel caso peggiore il nodo da cui partiamo è in fondo alla
   catena dei padri, e questa potrebbe contenere N nodi. Perciò la complessità sarà O(N)*O(N)=O(N^2);
  -la funzione (4) esegue due volte la funzione (3) e al massimo una volta la funzione (2).
   Ne segue che la complessità sarà 2*O(N^2)+O(N)=O(N^2).

  Dunque sommando tutti i contributi, il modulo avrà complessità O(N^2).




-"CostruzioneMST":
  Ho implementato una prima versione del primo pezzo del modulo. Dato che questo modulo si prospetta
  essere più lungo dei precedenti, ho deciso di suddividere anche la prima implementazione in più sessioni,
  perché ho stimato che non sarei riuscito a finirlo in una sola come è stato per i moduli precedenti.
  
  Questo modulo deve:

  -prendere tutti gli archi pesati e selezionare solo quelli appartenenti alla componente connessa maggiore;
  -ordinarli tutti in ordine crescente (perché questo servirà a facilitare la costruzione del MST);
  -per ordinarli, fra i vari algoritmi di sorting visti a lezione, ho penso di utilizzare il mergesort, 
   che grazie alla sua complessità O(nlogn) è molto usato per gestire strutture dati molto grandi come
   i dataset CAIDA di questo progetto;
  -selezionare, attraverso il controllo DSU, quali archi può usare per la costruzione del MST;
  -una volta costruito il MST, stampare i suoi archi per verifica.

  Con questo primo pezzo che ho implementato,


  IL MODULO:

  -seleziona gli archi del vettore archipesati che appartengono alla componente connessa maggiore,
   nella funzione "void selezionaArchiCCM"(2), grazie alla funzione ausiliaria "bool appartieneComponenteMaggiore"(1)
   e li salva nel vettore archiCCM;
  -stampa tutti gli archi pesati presenti in archiCCM per verifica.

  
  Si possono osservare quelle che finora sono le


  STRUTTURE DATI UTILIZZATE:

  -vector<ArcoPesato> per rappresentare tutti gli archi pesati appartenenti alla componente connessa maggiore.


  Si possono fare già adesso delle analisi sulla


  COMPLESSITÀ:

  Se N è il numero di nodi della componente connessa maggiore, e M il numero di archi pesati, allora:

  -la funzione (1) esegue una scansione lineare del vettore componente maggiore, perciò avrà complessità O(N);
  -la funzione (2) esamina ciascuno degli M archi e per ognuno chiama la funzione (1), 
   mentre l'altra operazione all'interno del for ha costo costante O(1).
   Dunque la complessità di questa funzione sarà O(M)*O(N)=O(M*N).

  Finora la complessità generale è O(M*N). 
